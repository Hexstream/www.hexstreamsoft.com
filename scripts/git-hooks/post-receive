GIT_WORK_TREE=/home/public/ git checkout -f

# Remove canonical self-links in production
sed -i -e '/<nav id="meta-nav"/{n;N;N;s/.*[Cc]anonical.*/<delete>/;
}; /<delete>/d' $(find /home/public/ -name '*.html')
